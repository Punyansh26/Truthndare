import { useEffect, useRef } from 'react';

export default function ConfettiBlast({ active, colors }) {
  // This is a placeholder — actual confetti is handled by useConfetti hook
  // This component can be used for a persistent canvas overlay if needed
  return null;
}
